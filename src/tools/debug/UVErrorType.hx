package tools.debug;

enum abstract UVErrorType(Int) {
	final E2BIG = -7; // "argument list too long"
	final EACCES = -13; // "permission denied"
	final EADDRINUSE = -48; // "address already in use"
	final EADDRNOTAVAIL = -49; // "address not available"
	final EAFNOSUPPORT = -47; // "address family not supported"
	final EAGAIN = -35; // "resource temporarily unavailable"
	final EAI_ADDRFAMILY = -3000; // "address family not supported"
	final EAI_AGAIN = -3001; // "temporary failure"
	final EAI_BADFLAGS = -3002; // "bad ai_flags value"
	final EAI_BADHINTS = -3013; // "invalid value for hints"
	final EAI_CANCELED = -3003; // "request canceled"
	final EAI_FAIL = -3004; // "permanent failure"
	final EAI_FAMILY = -3005; // "ai_family not supported"
	final EAI_MEMORY = -3006; // "out of memory"
	final EAI_NODATA = -3007; // "no address"
	final EAI_NONAME = -3008; // "unknown node or service"
	final EAI_OVERFLOW = -3009; // "argument buffer overflow"
	final EAI_PROTOCOL = -3014; // "resolved protocol is unknown"
	final EAI_SERVICE = -3010; // "service not available for socket type"
	final EAI_SOCKTYPE = -3011; // "socket type not supported"
	final EALREADY = -37; // "connection already in progress"
	final EBADF = -9; // "bad file descriptor"
	final EBUSY = -16; // "resource busy or locked"
	final ECANCELED = -89; // "operation canceled"
	final ECHARSET = -4080; // "invalid Unicode character"
	final ECONNABORTED = -53; // "software caused connection abort"
	final ECONNREFUSED = -61; // "connection refused"
	final ECONNRESET = -54; // "connection reset by peer"
	final EDESTADDRREQ = -39; // "destination address required"
	final EEXIST = -17; // "file already exists"
	final EFAULT = -14; // "bad address in system call argument"
	final EFBIG = -27; // "file too large"
	final EHOSTUNREACH = -65; // "host is unreachable"
	final EINTR = -4; // "interrupted system call"
	final EINVAL = -22; // "invalid argument"
	final EIO = -5; // "i/o error"
	final EISCONN = -56; // "socket is already connected"
	final EISDIR = -21; // "illegal operation on a directory"
	final ELOOP = -62; // "too many symbolic links encountered"
	final EMFILE = -24; // "too many open files"
	final EMSGSIZE = -40; // "message too long"
	final ENAMETOOLONG = -63; // "name too long"
	final ENETDOWN = -50; // "network is down"
	final ENETUNREACH = -51; // "network is unreachable"
	final ENFILE = -23; // "file table overflow"
	final ENOBUFS = -55; // "no buffer space available"
	final ENODEV = -19; // "no such device"
	final ENOENT = -2; // "no such file or directory"
	final ENOMEM = -12; // "not enough memory"
	final ENONET = -4056; // "machine is not on the network"
	final ENOPROTOOPT = -42; // "protocol not available"
	final ENOSPC = -28; // "no space left on device"
	final ENOSYS = -78; // "function not implemented"
	final ENOTCONN = -57; // "socket is not connected"
	final ENOTDIR = -20; // "not a directory"
	final ENOTEMPTY = -66; // "directory not empty"
	final ENOTSOCK = -38; // "socket operation on non-socket"
	final ENOTSUP = -45; // "operation not supported on socket"
	final EPERM = -1; // "operation not permitted"
	final EPIPE = -32; // "broken pipe"
	final EPROTO = -100; // "protocol error"
	final EPROTONOSUPPORT = -43; // "protocol not supported"
	final EPROTOTYPE = -41; // "protocol wrong type for socket"
	final ERANGE = -34; // "result too large"
	final EROFS = -30; // "read-only file system"
	final ESHUTDOWN = -58; // "cannot send after transport endpoint shutdown"
	final ESPIPE = -29; // "invalid seek"
	final ESRCH = -3; // "no such process"
	final ETIMEDOUT = -60; // "connection timed out"
	final ETXTBSY = -26; // "text file is busy"
	final EXDEV = -18; // "cross-device link not permitted"
	final UNKNOWN = -4094; // "unknown error"
	final EOF = -4095; // "end of file"
	final ENXIO = -6; // "no such device or address"
	final EMLINK = -31; // "too many links"
	final EHOSTDOWN = -64; // "host is down"
	final EOTHER = 0;
}
